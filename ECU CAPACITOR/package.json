{
  "name": "ecu-capacitor",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "powershell -Command \"New-Item -ItemType Directory -Path www -Force | Out-Null; Copy-Item -Path index.html,app.js,tailwind-config.js -Destination www -Force\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  }
}
